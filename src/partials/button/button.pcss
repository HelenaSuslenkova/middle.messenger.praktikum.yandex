.btn {
  width: 100%;
  height: 2.5rem;
  color: var(--white);
  background-color: var(--blue2);
  border-width: 0;
  border-radius: 0.5rem;

    &:hover {
      background-color: var(--blue1);
      color: var(--white);
      cursor: pointer;
    }
}

.submit-btn {
  margin-top: 2rem;
}
